{
    # NOTE: the directive was renamed from basicauth in Caddy 2.8.
    order rate_limit before basic_auth
}

bedebog.dk, localhost {
    rate_limit {remote.ip} 240r/m
    encode gzip
    reverse_proxy prayer:3457
}
